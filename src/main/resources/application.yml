spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dustinl?useUnicode=true&characterEncoding=utf8&characterSetResults=utf8&serverTimezone=GMT
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true
  thymeleaf:
    prefix: classpath:/static/

server:
  servlet:
    context-path: /

    session:
      timeout: 2h